# Normalize line endings — avoid CRLF/LF churn across machines.
* text=auto eol=lf

# Treat these as binary (no EOL conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
