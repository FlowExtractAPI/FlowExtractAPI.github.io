{
  "name": "flowextract-api-site",
  "version": "1.0.0",
  "private": true,
  "description": "FlowExtract API website + build-time docs/SEO generator",
  "scripts": {
    "sync": "node sync.js",
    "sync:all": "node sync.js --all",
    "check": "node sync.js --check"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
